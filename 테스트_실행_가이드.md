# 기프티쇼 비즈 QA 테스트 실행 가이드

## 목차
1. [환경 설정](#1-환경-설정)
2. [테스트 시나리오](#2-테스트-시나리오)
3. [테스트 실행](#3-테스트-실행)
4. [CLI 옵션 정리](#4-cli-옵션-정리)
5. [결과 확인](#5-결과-확인)
6. [문제 해결](#6-문제-해결)

---

## 1. 환경 설정

### 1-1. 초기 설정 (최초 1회)

```bash
cd ~/Desktop/giftishow_test
python3 -m venv venv
source venv/bin/activate
pip3 install -r requirements.txt
```

### 1-2. 가상환경 활성화 (매 세션마다)

```bash
cd ~/Desktop/giftishow_test
source venv/bin/activate
```

### 1-3. 필수 파일 확인

| 파일 | 설명 |
|------|------|
| `.env` | BASE_URL, Google Sheets URL, 로그인 정보 |
| `credentials.json` | Google API 서비스 계정 인증 |

### 1-4. venv 문제 시 재설정

```bash
cd ~/Desktop/giftishow_test
rm -rf venv && python3 -m venv venv && source venv/bin/activate
pip3 install -r requirements.txt
```

---

## 2. 테스트 시나리오

| TC_ID | 시나리오명 | 스크립트 | 상태 |
|-------|-----------|---------|------|
| GS_PRODUCT_001 | 카테고리 URL 및 상품 상세 페이지 | `test_category_urls.py` | 완료 |
| GS_PRODUCT_002 | 상품 검색 (키워드/인기 검색어) | `test_product_search.py` | 완료 |
| GS_AUTH_001~004 | 로그인/로그아웃 | `test_login.py` | Skip (2FA) |

### 지원 페이지 유형 (GS_PRODUCT_001)

| 유형 | URL 패턴 | 상품 감지 방식 |
|------|---------|--------------|
| 판촉 (panchok) | `/panchok/products/filtered?...` | `img[alt$='이미지']` |
| 비즈샵 (bizshop) | `/bizshop?categorySeq=...&brand=...` | `div.goods-item-article` |
| 배송상품 (bizDeliveryShop) | `/bizDeliveryShop?brand=...` | `div.goods-item-article` |

---

## 3. 테스트 실행

### 3-1. 카테고리 테스트 (GS_PRODUCT_001)

```bash
# 전체 카테고리 테스트
python3 -m pytest tests/test_category_urls.py -v

# 5개만 빠르게 확인
python3 -m pytest tests/test_category_urls.py -v --max 5

# 대분류만 테스트
python3 -m pytest tests/test_category_urls.py -v --level 1

# No.200부터 테스트 시작
python3 -m pytest tests/test_category_urls.py -v --start-from 200

# 이전 Fail/미테스트 항목만 재테스트
python3 -m pytest tests/test_category_urls.py -v --retry-fail

# Fail 항목 중 10개만 재테스트
python3 -m pytest tests/test_category_urls.py -v --retry-fail --max 10
```

### 3-2. 검색 테스트 (GS_PRODUCT_002)

```bash
# 전체 시나리오 (17 Steps)
python3 -m pytest tests/test_product_search.py -v

# 특정 키워드 테스트
python3 -m tests.test_product_search --keywords 스타벅스 커피
```

**전체 시나리오 구성**:

| Step | 내용 |
|------|------|
| 1-4 | '스타벅스' + 검색 버튼 클릭 |
| 5-8 | '생일' + Enter 입력 |
| 9-11 | 쿠폰 인기 검색어 클릭 |
| 12-14 | 판촉 인기 검색어 클릭 |
| 15-17 | 결과 없는 키워드 확인 |

### 3-3. 전체 테스트 / HTML 리포트

```bash
# 전체 테스트
python3 -m pytest tests/ -v

# HTML 리포트 생성
python3 -m pytest tests/ -v --html=reports/test_report.html --self-contained-html
```

---

## 4. CLI 옵션 정리

### test_category_urls.py

| 옵션 | 설명 | 예시 |
|------|------|------|
| `--max N` | 최대 N개만 테스트 | `--max 5` |
| `--level N` | 특정 레벨만 (1=대, 2=중, 3=소) | `--level 1` |
| `--start-from N` | No.N부터 시작 | `--start-from 583` |
| `--retry-fail` | Fail/미테스트 항목만 | `--retry-fail` |

### test_product_search.py

| 옵션 | 설명 | 예시 |
|------|------|------|
| `--full-scenario` | 전체 17 Steps | `--full-scenario` |
| `--keywords` | 특정 키워드 | `--keywords 스타벅스` |
| `--method` | 검색 방식 (enter/button) | `--method button` |
| `--headless` | 브라우저 숨김 | `--headless` |

### pytest 공통 옵션

| 옵션 | 설명 |
|------|------|
| `-v` | 상세 출력 |
| `-x` | 첫 실패 시 중단 |
| `--maxfail=N` | N번 실패 시 중단 |
| `--lf` | 마지막 실패 항목만 재실행 |
| `--reruns N` | 실패 시 N번 재시도 |
| `-n auto` | 병렬 실행 (CPU 코어 수) |

---

## 5. 결과 확인

### Google Sheets 자동 기록

| 시트 | 내용 |
|------|------|
| TestResults | 모든 테스트 결과 (Timestamp, TC_ID, Result, Duration) |
| Menu_tree_check_list | 카테고리별 Pass/Fail (K열), 오류 메시지 (L열) |
| TestSummary | 전체 현황 대시보드 |
| DailyTrend | 일별 성공률 추이 |

### 터미널 출력 예시

```
============================================================
Category URL Test Summary
============================================================
Total Categories: 30
Page Load Success: 30/30 (100%)
Detail Page Success: 28/30 (93%)
Full Pass: 28/30 (93%)
============================================================
```

---

## 6. 문제 해결

### "No module named ..." 에러

```bash
pip3 install -r requirements.txt
```

### "invalid session id" 에러

Chrome 크래시로 인한 세션 손실입니다. 세션 복구 로직이 자동으로 브라우저를 재시작합니다 (최대 5회). 이후 실패 항목은 `--retry-fail`로 재테스트합니다.

```bash
python3 -m pytest tests/test_category_urls.py -v --retry-fail
```

### Google Sheets 연결 오류

1. `credentials.json` 파일이 프로젝트 루트에 있는지 확인
2. Google Sheets 공유 설정에서 서비스 계정 이메일에 편집자 권한 부여

### 수동 로그인 (2FA)

```bash
python3 manual_login.py
```
브라우저에서 로그인 완료 후 터미널에서 Enter 입력 → 쿠키 자동 저장

---

## 프로젝트 구조

```
giftishow_test/
├── .env                         # 환경 설정
├── credentials.json             # Google API 인증
├── requirements.txt             # 의존성
├── pytest.ini                   # pytest 설정
├── tests/
│   ├── conftest.py              # pytest 픽스처/설정
│   ├── test_category_urls.py    # GS_PRODUCT_001
│   ├── test_product_search.py   # GS_PRODUCT_002
│   └── test_login.py            # GS_AUTH_001~004
├── pages/                       # Page Object Model
├── utils/                       # 유틸리티 (google_sheets, config, logger)
└── reports/                     # HTML 리포트
```

---

**버전**: 5.0 (2026-02-18) — bizDeliveryShop 지원, 문서 정리
