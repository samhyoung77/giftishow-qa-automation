# 🎯 기프티쇼 자동화 테스트 프로젝트 - 쉬운 설명

## 📌 이 프로젝트가 하는 일

**목적**: 기프티쇼 웹사이트를 자동으로 테스트하고, 결과를 Google Sheets에 기록해서 Looker Studio로 분석

```
웹사이트 자동 테스트 → 결과를 Google Sheets에 저장 → Looker Studio로 차트 보기
```

---

## 🏗️ 프로젝트 구조 - 간단 버전

```
giftishow_test/
│
├── 📁 pages/              ← 웹페이지를 코드로 표현 (페이지별로 클래스 만듦)
│   └── base_page.py      ← 모든 페이지의 기본 기능 (클릭, 입력 등)
│
├── 📁 tests/              ← 실제 테스트 코드 (pytest로 실행)
│   ├── conftest.py       ← pytest 설정 (자동 실행됨)
│   └── test_example.py   ← 예제 테스트
│
├── 📁 utils/              ← 도구 모음
│   ├── config.py         ← 설정 (URL, 브라우저 등)
│   ├── logger.py         ← 로그 기록
│   ├── google_sheets.py  ← Google Sheets에 결과 저장
│   └── create_sheets_template.py  ← Sheets 자동 생성
│
├── 📁 data/               ← 테스트 데이터 (계정 정보 등)
│   └── test_data.json
│
├── 📁 reports/            ← 테스트 결과 저장
│   ├── screenshots/      ← 실패 시 스크린샷
│   └── logs/             ← 로그 파일
│
├── 📄 .env               ← 환경 설정 (비밀번호 등)
├── 📄 requirements.txt   ← 필요한 라이브러리 목록
├── 📄 pytest.ini         ← pytest 설정
│
└── 🔧 배치 파일들         ← 클릭만 하면 실행
    ├── setup.bat         ← 처음 설치
    ├── run_tests.bat     ← 테스트 실행
    └── create_sheets.bat ← Google Sheets 만들기
```

---

## 🎬 동작 원리 - 3단계

### 1단계: 웹페이지 표현 (POM 패턴)
```
실제 웹페이지 → Python 클래스로 변환

예시:
로그인 페이지 = LoginPage 클래스
  - login() 메서드 → ID/PW 입력하고 로그인 버튼 클릭
  - is_logged_in() 메서드 → 로그인 성공했는지 확인
```

### 2단계: 테스트 작성
```python
# test_login.py
def test_login(driver):
    login_page = LoginPage(driver)  # 1. 로그인 페이지 객체 생성
    login_page.login("user", "pass")  # 2. 로그인 실행
    assert login_page.is_logged_in()  # 3. 성공했는지 확인
```

### 3단계: 자동 기록
```
pytest 실행 → 테스트 성공/실패 → 자동으로 Google Sheets에 기록
```

---

## 📂 주요 파일 설명 - 역할별로

### 🔧 설치/실행 파일 (클릭만 하면 됨)

| 파일 | 역할 | 언제 사용? |
|------|------|----------|
| **setup.bat** | 처음 설치 (Python 라이브러리 등) | 프로젝트 시작할 때 딱 1번 |
| **run_tests.bat** | 테스트 실행 | 테스트하고 싶을 때마다 |
| **create_sheets.bat** | Google Sheets 자동 생성 | Google Sheets 만들 때 1번 |

---

### ⚙️ 설정 파일

#### `.env` - 비밀 정보 저장
```env
# 이런 정보를 저장
GOOGLE_SHEET_URL=https://docs.google.com/...  ← 내 Google Sheets 주소
BASE_URL=https://biz.giftishow.com           ← 테스트할 웹사이트
TEST_USERNAME=my_id                          ← 테스트 계정
TEST_PASSWORD=my_password                    ← 테스트 비밀번호
BROWSER=chrome                               ← 사용할 브라우저
```

**왜 필요?**: 코드에 비밀번호를 직접 쓰면 위험하니까 여기에 저장

#### `credentials.json` - Google API 인증 키
```
Google Cloud Console에서 다운로드 받는 파일
이게 있어야 Google Sheets에 자동으로 쓸 수 있음
```

#### `requirements.txt` - 필요한 라이브러리 목록
```txt
selenium          ← 웹 자동화
pytest            ← 테스트 프레임워크
gspread           ← Google Sheets 연동
...
```

**사용법**:
```bash
pip install -r requirements.txt  ← 이 명령어로 한번에 설치
```

---

### 📝 코드 파일 - 실제 동작

#### `pages/base_page.py` - 기본 도구상자
```python
# 모든 페이지에서 공통으로 사용하는 기능
class BasePage:
    def click(locator)        # 버튼 클릭
    def input_text(locator, text)  # 텍스트 입력
    def get_text(locator)     # 텍스트 읽기
    def take_screenshot(name) # 스크린샷 찍기
    ...
```

**역할**: 웹 조작의 기본 기능 제공

#### `utils/config.py` - 설정 관리
```python
# .env 파일을 읽어서 사용하기 쉽게 만듦
class Config:
    BASE_URL = "https://biz.giftishow.com"
    BROWSER = "chrome"
    TEST_USERNAME = "my_id"
    ...
```

**역할**: `.env` 파일의 내용을 코드에서 `Config.BASE_URL` 형태로 사용

#### `utils/google_sheets.py` - Google Sheets 연동
```python
class GoogleSheetsReporter:
    def log_test_result(...)  # 테스트 결과 기록
    def update_summary()      # 요약 업데이트
    def add_daily_trend()     # 일별 통계 추가
```

**역할**: 테스트 결과를 Google Sheets에 자동 저장

#### `tests/conftest.py` - pytest 자동 설정
```python
# pytest가 자동으로 읽어서 실행하는 파일
# 여기에 정의된 fixture들이 모든 테스트에서 사용됨

@pytest.fixture
def driver():
    # 브라우저 자동 시작/종료

@pytest.fixture
def sheets_reporter():
    # Google Sheets 리포터 자동 생성
```

**역할**: 테스트 전후에 자동으로 실행되는 설정

---

## 🎯 실제 사용 흐름 - 단계별

### 첫 시작 (최초 1회)

#### 1️⃣ 프로젝트 설치
```bash
더블클릭: setup.bat
```
**일어나는 일**:
- Python 가상환경 생성
- 필요한 라이브러리 설치 (selenium, pytest 등)
- .env 파일 생성

#### 2️⃣ 환경 변수 설정
```bash
메모장 열기: .env
```
**수정할 내용**:
```env
# 실제 값으로 변경
TEST_USERNAME=실제_아이디
TEST_PASSWORD=실제_비밀번호
GOOGLE_SHEET_URL=내_구글시트_주소
```

#### 3️⃣ Google Cloud 설정 (복잡하지만 중요)
1. https://console.cloud.google.com 접속
2. 프로젝트 만들기
3. Google Sheets API 켜기
4. 서비스 계정 만들기
5. JSON 키 다운로드 → `credentials.json`으로 저장

**왜 필요?**: 프로그램이 자동으로 Google Sheets에 쓰려면 권한이 필요

#### 4️⃣ Google Sheets 만들기
```bash
더블클릭: create_sheets.bat
```
**일어나는 일**:
- Google Sheets 자동 생성
- 4개 시트 만들어짐:
  - TestCase_Template (테스트 케이스 정의)
  - TestResults (실행 결과)
  - TestSummary (요약 대시보드)
  - DailyTrend (일별 통계)

---

### 일상적인 사용 (매번)

#### 테스트 실행
```bash
더블클릭: run_tests.bat
```

**일어나는 일**:
1. Chrome 브라우저 자동 실행
2. 기프티쇼 사이트 접속
3. 테스트 실행 (로그인, 상품 조회 등)
4. 성공/실패 결과를 Google Sheets에 자동 기록
5. HTML 리포트 자동 생성
6. 브라우저 종료

**결과 확인**:
- 화면에 성공/실패 표시
- `reports/report.html` 자동으로 열림
- Google Sheets에 결과 추가됨

---

## 💡 핵심 개념 설명

### 1. POM (Page Object Model) 패턴이란?

**나쁜 예시** (POM 없이):
```python
def test_login():
    driver.find_element(By.ID, "username").send_keys("user")
    driver.find_element(By.ID, "password").send_keys("pass")
    driver.find_element(By.ID, "login_btn").click()
    # 로그인 코드가 테스트마다 중복됨
```

**좋은 예시** (POM 사용):
```python
# pages/login_page.py
class LoginPage:
    def login(self, user, password):
        self.input_text(self.USERNAME, user)
        self.input_text(self.PASSWORD, password)
        self.click(self.LOGIN_BTN)

# test_login.py
def test_login():
    login_page = LoginPage(driver)
    login_page.login("user", "pass")  # 간단!
```

**장점**:
- 로그인 코드를 한 곳에만 작성
- 웹페이지 변경 시 LoginPage만 수정하면 됨
- 테스트 코드가 읽기 쉬움

---

### 2. pytest fixture란?

**일반 코드**:
```python
def test_1():
    driver = start_browser()  # 반복
    # 테스트
    driver.quit()

def test_2():
    driver = start_browser()  # 반복
    # 테스트
    driver.quit()
```

**fixture 사용**:
```python
@pytest.fixture
def driver():
    driver = start_browser()
    yield driver  # 테스트에 제공
    driver.quit()  # 자동 종료

def test_1(driver):  # 자동으로 받음
    # 테스트

def test_2(driver):  # 자동으로 받음
    # 테스트
```

**장점**:
- 중복 제거
- 자동으로 설정/정리

---

### 3. Google Sheets 연동 흐름

```
테스트 실행
    ↓
pytest가 conftest.py 읽음
    ↓
sheets_reporter fixture 자동 생성
    ↓
테스트 완료 후 pytest_runtest_makereport 훅 실행
    ↓
GoogleSheetsReporter.log_test_result() 호출
    ↓
Google Sheets에 결과 기록
```

**코드로 보면**:
```python
# conftest.py에서
@pytest.hookimpl(tryfirst=True, hookwrapper=True)
def pytest_runtest_makereport(item, call):
    # 테스트 끝나면 자동 실행
    if sheets_reporter:
        sheets_reporter.log_test_result(...)
```

---

## 🗂️ Google Sheets 구조

### TestCase_Template (테스트 시나리오 정의)
```
| TC ID         | Category   | Scenario Name | ... |
|---------------|------------|---------------|-----|
| GS_LOGIN_001  | Auth       | 정상 로그인     | ... |
| GS_LOGIN_002  | Auth       | 실패 로그인     | ... |
| GS_PRODUCT_001| Product    | 상품 검색      | ... |
```

**역할**: 어떤 테스트를 할지 정의

---

### TestResults (실행 결과)
```
| Timestamp           | TC ID        | Result | Duration | ... |
|---------------------|--------------|--------|----------|-----|
| 2026-02-03 14:30:25 | GS_LOGIN_001 | PASS   | 3.24s    | ... |
| 2026-02-03 14:30:29 | GS_LOGIN_002 | PASS   | 2.85s    | ... |
| 2026-02-03 14:30:32 | GS_PRODUCT_001| FAIL  | 1.52s    | ... |
```

**역할**: 실행할 때마다 결과 자동 추가

---

### TestSummary (대시보드)
```
전체 요약
- 총 테스트: 50건
- 성공: 45건
- 실패: 5건
- 성공률: 90%

카테고리별
- Authentication: 10건 (10 성공, 0 실패)
- Product: 20건 (18 성공, 2 실패)
...

최근 실패 로그
- 2026-02-03 14:30:32 | GS_PRODUCT_001 | Element not found
```

**역할**: 한눈에 현황 파악

---

## 🔄 전체 흐름 예시

### 시나리오: 로그인 테스트

#### 1. 테스트 케이스 작성 (Google Sheets)
```
TestCase_Template 시트에 추가:
TC ID: GS_LOGIN_001
Category: Authentication
Scenario: 정상 로그인
Steps: 1. ID 입력 2. PW 입력 3. 로그인 버튼 클릭
Expected: 메인 페이지 이동
```

#### 2. Page Object 작성
```python
# pages/login_page.py
class LoginPage(BasePage):
    USERNAME = (By.ID, "username")
    PASSWORD = (By.ID, "password")
    LOGIN_BTN = (By.ID, "login_btn")

    def login(self, user, password):
        self.input_text(self.USERNAME, user)
        self.input_text(self.PASSWORD, password)
        self.click(self.LOGIN_BTN)

    def is_logged_in(self):
        return "main" in self.get_current_url()
```

#### 3. 테스트 작성
```python
# tests/test_login.py
@pytest.mark.tc_id("GS_LOGIN_001")
@pytest.mark.page("Login Page")
@pytest.mark.scenario("정상 로그인")
def test_successful_login(driver, test_data):
    # Given
    login_page = LoginPage(driver)
    login_page.navigate_to("https://biz.giftishow.com/login")

    # When
    login_page.login(
        test_data['valid_user']['username'],
        test_data['valid_user']['password']
    )

    # Then
    assert login_page.is_logged_in()
```

#### 4. 실행
```bash
더블클릭: run_tests.bat
```

#### 5. 자동으로 일어나는 일
```
1. Chrome 실행
2. 로그인 페이지 접속
3. ID/PW 입력
4. 로그인 버튼 클릭
5. 메인 페이지 이동 확인
6. 성공! → Google Sheets에 기록
   - Timestamp: 2026-02-03 14:30:25
   - TC ID: GS_LOGIN_001
   - Result: PASS
   - Duration: 3.24s
7. Chrome 종료
8. HTML 리포트 생성
```

---

## 🎓 각 파일을 언제 수정하나?

| 파일 | 수정 시기 | 예시 |
|------|----------|------|
| `.env` | 설정 변경 시 | URL 변경, 계정 변경 |
| `pages/*.py` | 새 페이지 추가 시 | 결제 페이지 추가 → payment_page.py |
| `tests/*.py` | 새 테스트 추가 시 | 결제 테스트 → test_payment.py |
| `data/test_data.json` | 테스트 데이터 추가 시 | 새 상품 정보 추가 |
| `utils/config.py` | 새 설정 항목 추가 시 | 타임아웃 값 추가 |

---

## 🚀 빠른 시작 체크리스트

### 준비 단계
- [ ] Python 3.9+ 설치 확인
- [ ] `setup.bat` 실행
- [ ] `.env` 파일 편집 (계정 정보 입력)

### Google 설정 (복잡하지만 1번만)
- [ ] Google Cloud Console에서 프로젝트 생성
- [ ] Google Sheets API + Drive API 활성화
- [ ] 서비스 계정 생성
- [ ] `credentials.json` 다운로드 및 저장
- [ ] Google Sheets 생성
- [ ] 서비스 계정과 공유 (편집자 권한)
- [ ] `.env`에 Sheet URL 입력

### 시트 생성
- [ ] `create_sheets.bat` 실행
- [ ] Google Sheets에서 4개 시트 확인

### 테스트 실행
- [ ] `run_tests.bat` 실행
- [ ] HTML 리포트 확인
- [ ] Google Sheets에 결과 기록 확인

---

## ❓ 자주 묻는 질문

### Q1. 왜 이렇게 복잡하게 만들었나요?
**A**: 처음엔 복잡해 보이지만 장점이 많습니다:
- **재사용**: 한번 작성하면 계속 사용
- **유지보수**: 웹페이지 변경 시 Page Object만 수정
- **자동화**: 테스트 → 결과 기록 → 리포트까지 자동
- **협업**: 팀원들과 Google Sheets 공유

### Q2. POM 패턴을 꼭 써야 하나요?
**A**: 안 써도 되지만 권장:
- 테스트 1-2개: POM 없이도 OK
- 테스트 10개 이상: POM 쓰면 관리 쉬움
- 장기 프로젝트: POM 필수

### Q3. Google Sheets 연동이 어려워요
**A**: 단계별로:
1. 먼저 `.env` 파일 없이 로컬 테스트만
2. 테스트가 잘 되면 Google Sheets 설정
3. `GOOGLE_SHEETS_GUIDE.md` 참고

### Q4. 실패하면 어떻게 디버깅하나요?
**A**:
1. `reports/screenshots/` - 실패 시 스크린샷 자동 저장
2. `reports/logs/` - 로그 파일 확인
3. pytest 실행 시 `-v -s` 옵션으로 상세 출력

---

## 📚 더 알아보기

### 초보자용
- `README.md` - 전체 개요
- `WINDOWS_SETUP.md` - Windows 환경 설정

### 중급자용
- `기프티쇼_자동화_테스트_프로젝트.md` - 상세 설계
- `GOOGLE_SHEETS_GUIDE.md` - Google Sheets 연동

### 고급자용
- `pages/base_page.py` - BasePage 구현
- `tests/conftest.py` - pytest 훅
- `utils/google_sheets.py` - API 연동

---

## 🎉 요약

1. **이 프로젝트는**: 웹사이트 자동 테스트 + Google Sheets 자동 기록
2. **POM 패턴**: 페이지를 클래스로 표현해서 재사용
3. **pytest**: 테스트 실행 프레임워크
4. **Google Sheets**: 결과를 자동으로 저장해서 분석
5. **배치 파일**: 클릭만으로 설치/실행

**핵심**: 한번 설정하면 `run_tests.bat` 클릭만으로 모든 게 자동!

---

**이해 안 되는 부분이 있으면 언제든 물어보세요!**
